# secrets
.env
.env.*
!.env.example

# go — `go build` writes ./shipline; `go test -c` writes *.test
/shipline
*.test
bin/
dist/
*.exe
coverage.out

# terraform (when we get there)
.terraform/
*.tfstate
*.tfstate.backup
errored.tfstate
*.tfvars
!.tfvars.example

# portfolio capture (redacted, still not committed)
.portfolio/sessions/
